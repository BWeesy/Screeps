// For each source
// Get the position of the source
// Find the squares around that position
// Remove the squares that are walls
// Get the max number of containers
// Fill candidates minus 1 with containers

const buildingPlanner = {
  buildContainers: (spawner: StructureSpawn): void => {
    console.log(`Building containers for ${spawner.name}`);
  }
};

export default buildingPlanner;
